{
  "task_id": "task-riot-api-key-handling",
  "task_description": "Fix the API route error handling to cover the missing RIOT_API_KEY case, so that all key-related failures (missing, expired, or invalid) return the user-friendly 503 response instead of a generic 500 error.",
  "status": "in_progress",
  "feature_order": ["FEAT-001"],
  "blocked_reason": null
}
